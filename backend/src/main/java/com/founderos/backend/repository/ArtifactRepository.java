package com.founderos.backend.repository; import com.founderos.backend.domain.Artifact; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface ArtifactRepository extends JpaRepository<Artifact, UUID>{List<Artifact> findByProjectIdOrderByVersionAsc(UUID projectId); Optional<Artifact> findByProjectIdAndTypeAndVersion(UUID projectId,String type,int version);}
